language: python

dist: trusty

python:
  - 2.7.10

install:
  - pip install --quiet tmdbsimple
  - pip install --quiet plexapi
  - pip install --quiet future
  - pip install --quiet pyyaml
  - pip install --quiet dotmap

script:
  - cd fylm; python -m unittest tests